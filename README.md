# nube1

Repositorio puente entre **PC de casa** (Claude Code local) y **PC de trabajo** (Claude Code web).

- `CLAUDE.md` → reglas de estilo y protocolo. Se carga solo en cada sesión.
- `docs/ESTADO.md` → bitácora: qué quedó pendiente en la sesión anterior.

**Regla de oro**: `git pull` al empezar, `git push` al terminar.
